<===> input.scss
@use "sass:math";
@use "sass:meta";
// Units cancel even if they're totally unknown to Sass.
$number: 1foo * math.div(math.div(1bar, 1baz), 1qux);
a {
  b: meta.inspect($number * 1baz);
}

<===> output.css
a {
  b: calc(1foo * 1bar / 1qux);
}
