<===> input.scss
@use "sass:list";
@use "sass:meta";

form {
  $selector: list.nth(&, 1);
  sel: meta.inspect($selector);
  $selector: list.nth($selector, 1);
  sel: meta.inspect($selector);
}
<===> output.css
form {
  sel: form;
  sel: form;
}
