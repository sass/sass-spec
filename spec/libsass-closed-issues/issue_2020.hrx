<===> input.scss
@use "sass:meta";

form {
  $selector: nth(&, 1);
  sel: meta.inspect($selector);
  $selector: nth($selector, 1);
  sel: meta.inspect($selector);
}
<===> output.css
form {
  sel: form;
  sel: form;
}
