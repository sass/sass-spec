<===> input.scss
$input: "dquoted";
.result {
  output: "[\#{"dquoted"}]";
  output: "\#{"dquoted"}";
  output: '\#{"dquoted"}';
  output: "['\#{"dquoted"}']";
}

<===> output.css
.result {
  output: "[#{" dquoted "}]";
  output: "#{" dquoted "}";
  output: '#{"dquoted"}';
  output: "['#{" dquoted "}']";
}

<===> output-libsass.css
.result {
  output: "[#{" dquoted "}]";
  output: "#{" dquoted "}";
  output: '\#{"dquoted"}';
  output: "['#{" dquoted "}']";
}
